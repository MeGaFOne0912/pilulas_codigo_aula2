import random
cotacao = float(input('Cotação atual do Dolar: '))
variacao = random.uniform(-0.015, 0.015)
nova_variacao = cotacao * (1 + variacao)
print(f'Variação Simulada: {variacao:.3%}')
print(f'Nova Cotação:{nova_variacao: .2f}')